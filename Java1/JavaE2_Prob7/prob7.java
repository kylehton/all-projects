import javax.swing.JFrame;
import java.awt.Color;
public class prob7
{
     public static void main(String[] args) {
          JFrame myFrame = new JFrame();
          myFrame.setSize(100,200);
          Color greenColor = new Color(0,255,0);
          myFrame.setBackground(greenColor);
     }
}